<!DOCTYPE html>
<html lang="en">
    <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blog This</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" integrity="sha384-JcKb8q3iqJ61gNV9KGb8thSsNjpSL0n8PARn9HuZOnIxN0hoP+VmmDGMN5t9UJ0Z" crossorigin="anonymous">
    <link rel="stylesheet" href="/stylesheets/style.css">
    </head>
    <body>
        {{!-- <header> --}}
            <nav class="navbar navbar-dark text-white bg-primary">
                <h1>Blog This</h1>
                <ul class="list-group">
                    <li>
                        <a class="btn btn-success" href="/">Home</a>
                        {{#if loggedIn}}
                            <a class="btn btn-success" href="/dashboard">Dashboard</a>
                            <button class="btn btn-success" id="logout">Logout</button>
                        {{else}}
                            <a class="btn btn-success" href="/login">Login</a>
                        {{/if}}
                    </li>
                </ul>
            </nav>
        {{!-- </header> --}}
        <main>
            {{{ body }}}
        </main>
        
        <footer class="d-flex justify-content-center bg-primary fixed-bottom p-2">
            <h2 class="text-white">&copy {{current_year}} wubbleTea</h2>
        </footer>
    {{#if loggedIn}}
    <script src="/javascript/logout.js"></script>
    {{/if}}
    </body>

</html>